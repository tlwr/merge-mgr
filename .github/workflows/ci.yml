---
name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    steps:
      - name: setup
        uses: actions/setup-go@v4
        with:
          go-version: 1.19

      - name: checkout
        uses: actions/checkout@v3

      - name: dep
        run: go get

      - name: make
        run: make
        shell: bash
